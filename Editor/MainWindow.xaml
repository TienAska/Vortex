<Window x:Class="Editor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Editor"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <!--<Border Grid.Row="0" Grid.RowSpan="10" Background="Green"/>-->
            <local:RenderSurfaceView Grid.Row="0" Background="HotPink"/>
            <GridSplitter Grid.Row="1" Background="Black" HorizontalAlignment="Stretch" VerticalAlignment="Center" Height="5" Panel.ZIndex="1"/>
            <Border Grid.Row="2" Background="Aqua"/>
        </Grid>
        <GridSplitter Grid.Column="1" Background="Black" HorizontalAlignment="Center" VerticalAlignment="Stretch" Width="5" Panel.ZIndex="1"/>
        <Border Grid.Column="2" Background="Bisque"/>
    </Grid>
</Window>
